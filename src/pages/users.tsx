import Layout from "@/components/layout";
import UserCard from "@/components/user-card";

const Users = () => {
  return (
    <Layout title="users">
      <UserCard />
    </Layout>
  );
};

export default Users;
